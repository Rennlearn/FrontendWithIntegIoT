##
# PillNow Local IoT Backend (backend/server.js) environment
#
# Copy this to backend/backend.env and fill the values.
##

# --- Core services ---
PORT=5001
# MQTT broker URL for local IoT
# - If Mosquitto runs on the SAME Mac as backend/server.js: mqtt://127.0.0.1:1883
# - If Mosquitto runs on another machine: mqtt://<broker-ip>:1883
MQTT_BROKER_URL=mqtt://127.0.0.1:1883
VERIFIER_URL=http://127.0.0.1:8000/verify

# --- Email (optional) ---
# If enabled, the backend sends email for:
# - schedule alarm trigger
# - pill mismatch detection
EMAIL_ENABLED=false

# Where to send alerts/reminders
EMAIL_TO=

# The "From" address (often must match your SMTP account)
EMAIL_FROM=

# SMTP settings
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=


